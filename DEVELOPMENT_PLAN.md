# NovaEpub 开发计划（迭代路线）

> 定位：**轻量、可视化、更好上手的 Sigil**（EPUB 编辑器），不是 Notion。
> Notion 仅作为 UI 参考；核心是「自定义格式模板 + 点选套用 + 所见即所得」。

---

## 一、现状（当前已完成）

- **项目**：纯前端 SPA，Vue 3 + Vite + Pinia + Vue Router + TipTap + JSZip + Tailwind。
- **书架/导入**：书目列表、新建空白书、拖拽/上传导入 `.epub`，解析 OPF / NCX。
- **编辑器**：三栏（左目录 / 中画布 / 右样式模板+属性）。
  - 自定义格式模板（HTML 片段 + `$1` 占位符），可视化表单 + 原始 HTML/CSS 两种编辑。
  - 选中文本/图片后右键（长按）套用模板；块级模板整块替换不留空段落。
  - 图片：工具栏上传 + Ctrl+V 粘贴截图；导出打包到 `OEBPS/images/`。
  - 目录：新增/删除/重命名/上下移，新增章节自动编号「第X章」。
- **元数据**：书名/作者/出版日期/语言/UUID 弹窗。
- **预览**：375px 居中 iframe，锁定画布。
- **导出**：生成 OPF + NCX + XHTML + CSS，图片打包、模板 `<style>` 编译进 CSS，mimetype 首项不压缩。
- **UI**：Notion 式中性配色（炭黑 `#37352F` / 暖灰 `#F7F6F3`），无蓝紫无刺眼红；图标已接入（`/icon.jpg`）。
- **质量**：Vitest 102 项测试通过，`npm run build` 通过。
- **P0 完成情况**：撤销/重做、zip 闭环、容错导入已完成；后续补齐了崩溃恢复（IndexedDB 草稿）、导出前 EPUB 结构自检、封面更换 UI。
- **P1 完成情况**：接近真实阅读器的整书预览、章节拆分/合并/拖拽重排、全局查找替换、TXT/Markdown 导入、图片压缩均已落地。

---

## 二、已知痛点（按严重度）

### 1. 导入的 EPUB 会丢图片（最严重）
- 现状：解析时只取正文文本，`<img src="相对路径/...">` 没有把源文件里的图片一起搬进书。
- 后果：导入一本书后所有插图失效；导出也不会恢复。

### 2. localStorage 存 dataURL 图片，易超配额、卡顿
- 图片以 `data:image/...;base64` 直接塞进章节内容（`chapter.content`），一个几百 K 的图会让每章内容很大。
- 大书/多图会撑爆 localStorage，导出/预览也慢。
- 需要：图片压缩/重采样、或单独存储（IndexedDB）+ 引用 ID。

### 3. 模板被删除后，已套用的 class 样式会失效
- 模板若用 `<style>+class` 方式，删除该模板后，正文里已套用的 class 在导出时没有对应 CSS。
- 需要：套用时把样式固化（内联）或记录“已使用样式”，避免删除模板破坏正文。

### 4. 移动端长按套用不够稳
- 目前依赖 `contextmenu` 事件，桌面 OK；移动端浏览器长按触发不稳定。
- 需要显式长按计时（touchstart/touchend） + 防原生菜单。

### 5. 预览只显示单章、无翻页
- 当前是单章 iframe；更像阅读器应支持整书连续阅读/翻页 + 目录跳转。

### 6. 章节排序只有箭头，不够顺滑
- 期望拖拽排序。

### 7. 编辑体验偏基础
- 无表格/链接/上下标等；格式化工具少；键盘快捷键不全。

---

## 三、迭代优先级

### P0（先解决“能用”）
1. **导入保留图片**：从源 EPUB 解包时把图片提取出来，存进书，`img` 的 `src` 重写为书内资源。
2. **图片压缩/重采样**：插入/粘贴/导入时对图片降采样（canvas），限制单图大小。
3. **图片存储与引用**：章节内容里的 `src` 改成书内图片引用（如图片 UUID），图片二进制脱离正文；先用「书内图库 + 导出打包」方案，避免动大手术。
4. **模板样式生命周期**：套用模板时把样式落地（内联或随内容带 CSS），确保删除模板不破坏已用内容。

### P1（做体验）
5. **书内模板 vs 全局模板**：支持“每本书自带样式库 + 可从全局导入/复制”。
6. **拖拽排序**：目录章节拖拽调整顺序。
7. **移动端长按优化**：`touchstart` 长按计时弹出模板菜单。
8. **表格/链接/行内格式**：接入 TipTap table/link 等扩展。
9. **预览整书/翻页**：连续阅读 + 目录跳转 + 上下页。
10. **更多内置模板**：题记、分隔线、脚注、注释、图片说明等模板库。

### P2（强化与打磨）
11. **EPUB 3 兼容**：可选生成 `nav.xhtml` 与 EPUB 3 版本；或至少校验导出的合法性（内置 epubcheck 简化版）。
12. **封面与更多元数据**：自动/手动设封面，增加出版社、简介、主题、版权等字段。
13. **导入导出项目备份**：整书导出为 JSON / 打包成可再导入的 `.novaepub` 以便备份迁移。
14. **索引数据库迁移**：若图片/书规模上来，迁移到 IndexedDB，规避 localStorage 配额。
15. **工程化**：ESLint + Prettier、按路由分包（TipTap/JSZip 懒加载）、更多端到端测试。
16. **响应式/移动布局**：窄屏时侧栏收进抽屉，画布最大化。

---

## 四、分阶段路线图（建议顺序）

### 阶段 A —— 图片与数据（P0）
- **A1**：图片解包持久化
  - `epubParser`：读取源 EPUB 中 `manifest` 所有 `image/*` 资源，存为书级图库。
  - 章节 `img` 的 `src` 重写为书内引用（如 `images/第x章/图1` 或 UUID）。
  - `epubExporter`：按书内图库 + 正文引用输出 `OEBPS/images/`。
  - 验收：导入一本带插图的 epub → 预览/导出都能看到图。
- **A2**：图片压缩/重采样
  - 插入/粘贴/导入时用 `canvas` 降采样到合理宽度（如最大 1600px），JPEG 质量 ~0.8。
  - 验收：插入一张 5MB 图，存入后明显变小，导出不乱码。
- **A3**：存储方案
  - 中间态：图片存 `bookStore.images`（对象数组），正文引用图片 id；导出时打包。
  - 可选：接入 IndexedDB 存图片二进制，`localStorage` 只存元数据。
  - 验收：多图大书能正常编辑与导出，不超 localStorage。

### 阶段 B —— 模板系统完善（P0+P1）
- **B1**：模板样式固化
  - 套用模板时，若含 `<style>`，把样式完整保留在正文内联/随内容存储；删除模板不影响已用内容。
  - 验收：套用某 class 模板 → 删除该模板 → 正文样式仍在、导出正常。
- **B2**：书内模板库
  - 新模型：`书中可存一套模板`，编辑时优先用书内模板，可一键“从全局复制/导入”。
  - 验收：不同书可有不同标题/正文/图片样式。
- **B3**：模板库扩充
  - 增加：题记、分隔线、脚注、图片说明、双栏（模拟）、首字下沉变体等。
  - 验收：点选更多模板可套用到文本/图片。

### 阶段 C —— 编辑与预览体验（P1）
- **C1**：拖拽排序章节。
- **C2**：长按模板菜单（移动端）。
- **C3**：表格/链接/行内扩展。
- **C4**：预览整书、翻页、目录跳转。
- **C5**：键盘快捷键 + 工具栏完善。

### 阶段 D —— 导出与兼容（P1/P2）
- **D1**：EPUB 3 版本（可选）。
- **D2**：内置 epub 校验/自检（缺失 manifest、空章节、重复 id 等）。
- **D3**：封面与更多元数据字段。
- **D4**：项目备份/导入（`.novaepub`）。

### 阶段 E —— 工程化（P2）
- **E1**：ESLint + Prettier + 提交规范。
- **E2**：按路由/按需分包（TipTap、JSZip、封面解码懒加载）。
- **E3**：更多端到端测试 + CI。

---

## 五、明天需要一起确认的关键决策

1. **图片存储方案**：
   - 方案A：图片继续存 base64 但压缩、按章引用（改动小）。
   - 方案B：引入 IndexedDB 存二进制，正文只存图片引用（更稳，但改动大）。
   - 倾向 **B**，或先做 A 做过渡。

2. **模板归属**：
   - 全局模板（所有书共用） vs 书内模板（每本书自带，可全局导入）。
   - 倾向 **书内模板 + 全局为模板池**（更像 Sigil 每本书独立 stylesheet）。

3. **模板样式落地方式**：
   - 套用即内联样式（简单、自包含，导出/预览一定对）。
   - 还是“类名 + 书内 stylesheet”（更像 Sigil，但要处理删除/编辑联动）。
   - 倾向 **内联为主，复杂模板再走书内 stylesheet**。

4. **EPUB 版本**：
   - 现阶段只做 EPUB 2 + NCX 是否够用？还是要上 EPUB 3（`nav.xhtml`）。
   - 托底：导出保持 EPUB 2，另加“校验自检”。

5. **预览形态**：
   - 单章手机预览（现状）→ 是否升级为“整本连续阅读 + 目录跳转 + 翻页”。

---

## 六、测试与验收约定

- 每个 P0 项必须有对应的自动化测试（单元 + 组件/端到端）。
- 关键链路回归：
  1. 新建书 → 进编辑器 → 写内容 → 预览 → 导出，不白屏。
  2. 导入带图 epub → 预览/导出，图片可见。
  3. 套用模板 → 删除模板 → 导出，样式不丢。
  4. 多图书 → 编辑/导出，不卡不超限。
- 交付前必须 `npm run build` + `npm test` 全绿。

---

## 七、立即可做（明天可先动）

- **P0-1 导入保留图片**：`epubParser` 提取 manifest 图片 → 书内图库 → 重写 src → `epubExporter` 打包。这是当前最影响“像不像 Sigil”的一环。
- **P0-2 图片压缩**：复用“插入/粘贴”入口做 canvas 降采样。
- **P0-3 模板样式固化**：让套用后的样式不依赖模板是否还存在。
